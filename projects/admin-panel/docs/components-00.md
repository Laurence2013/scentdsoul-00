# Refactor dashboard.component.ts

IF there are repeateded code at editItem() and addNewItem() in dashboard.component.ts, THEN can you refactor and create a private method called
item_updates()

# Store Image into Firebase Emulator Storage and add the image path string into 
